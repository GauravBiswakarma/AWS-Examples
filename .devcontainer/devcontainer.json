{
  "name": "AWS Examples",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  
  "postCreateCommand": "cd /workspace && curl 'https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip' -o 'awscliv2.zip' && unzip -q awscliv2.zip && sudo ./aws/install && cd $PWD",
  
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  
  "remoteEnv": {
    "AWS_CLI_AUTO_PROMPT": "on-partial"
  }
}
